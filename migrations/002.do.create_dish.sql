CREATE TABLE dish (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    price INTEGER NOT NULL,
    restaurant_id INTEGER
        REFERENCES restaurant(id) ON DELETE CASCADE NOT NULL
);

