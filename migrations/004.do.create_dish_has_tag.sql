CREATE TABLE dish_has_tag (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    dish_id INTEGER
        REFERENCES dish(id) ON DELETE CASCADE NOT NULL,
    tag_id INTEGER
        REFERENCES tag(id) ON DELETE CASCADE NOT NULL,
    UNIQUE(dish_id, tag_id)
);